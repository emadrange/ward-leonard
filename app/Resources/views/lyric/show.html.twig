{% extends 'base.html.twig' %}

{% block body %}
    <h1>Lyric</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lyric.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ lyric.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ lyric.content }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('lyric_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('lyric_edit', { 'id': lyric.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
